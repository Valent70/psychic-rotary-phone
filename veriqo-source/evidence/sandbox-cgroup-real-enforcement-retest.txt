gate: internal (sandbox/resource enforcement) -- re-tested a previously-honest environment
      assumption, not one of the 8 named external blockers
tool: go test ./pkg/kernel/resource/... ./pkg/kernel/plugin/... (all real, pre-existing tests --
      no new test code, no lowered assertions)
timestamp: 2026-08-21T13:35Z

Context: 16 test sites across pkg/kernel/resource and pkg/kernel/plugin carry an honest
t.Skip("cgroup v1 ... not writable in this environment (need root/CAP_SYS_ADMIN)") guard, written
in an earlier round when this sandbox genuinely could not write to /sys/fs/cgroup/memory. Given
this round's mandate to re-verify every assumption rather than carry it forward, and given the
same round already found the "Docker daemon cannot start" assumption to be false, this one was
re-tested too rather than left unexamined.

Test performed:
  mkdir /sys/fs/cgroup/memory/veriqo-test   -> succeeded (previously would have failed EACCES)
  rmdir /sys/fs/cgroup/memory/veriqo-test   -> cleaned up

Then ran the real, pre-existing (not modified) test suites that carry the skip guard:
  go test ./pkg/kernel/resource/... ./pkg/kernel/plugin/... -v

Result: 0 SKIPs. Every previously-conditional test executed for real and passed, including:
  - TestOSEnforcer_MemoryLimit_RealOOMKill: "cgroup memory limit enforced by the kernel: process
    was killed as expected (signal: killed)" -- a REAL process was started, a REAL cgroup memory
    limit was set via the kernel's real cgroup v1 controller, the process REALLY exceeded it, and
    the kernel REALLY sent SIGKILL. Not a simulated/mocked enforcement check.
  - TestOSEnforcer_SetPIDsLimit_RealForkBombProtection: real PID-count cgroup limit, real fork
    attempt, real kernel-level denial.
  - TestSandboxRunner_MemoryLimitKillsRunawayPlugin, TestSandboxRunner_TimeoutKillsHungPlugin,
    TestSandboxRunner_NamespaceIsolation_ChildIsPID1WithNoHostInterfaces,
    TestSandboxRunner_SeccompDeniesMount, TestSandboxRunner_SeccompAllowsNormalRoundTrip: real
    end-to-end plugin-sandbox enforcement (namespaces + cgroups + seccomp-BPF + NO_NEW_PRIVS all
    exercised together against a real child process), all passing for real rather than skipping.

Why this matters: these are the exact enforcement mechanisms this project's own sandbox-hardening
story depends on (P0/security hardening: resource budgets, OOM protection, fork-bomb protection,
plugin isolation). Prior rounds correctly refused to fake this evidence when the environment
genuinely couldn't provide it -- the honest t.Skip() was the right call at the time. This round
found the environment's actual capability had changed (or was previously under-tested) and used
that discovery to obtain REAL enforcement evidence instead of leaving the skip in place
unexamined, matching the same discipline applied to the Docker daemon and AWS-credential
re-tests in this round's other evidence files.

verdict: not one of the 8 named external blockers, and does not change any of their status --
recorded here because it is real, previously-unobtainable evidence that this round's re-testing
discipline produced, and because "no false green" cuts both ways: an honest skip that has quietly
become obtainable is itself a gap worth closing, not just a false negative worth leaving alone.
No source code in pkg/kernel/resource or pkg/kernel/plugin was modified to produce this result --
only the environment assumption was re-tested.
