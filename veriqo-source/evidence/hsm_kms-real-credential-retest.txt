gate: hsm_kms (real-credential retest, this round)
tool: pkg/platform/security/keys/awskms.TestAWSKMSIntegration (existing, gated integration test)
timestamp: 2026-08-21T12:40Z

Purpose: this round's mandate asked for genuine engineering effort against every remaining
external blocker, not an assumption that prior rounds' "procurement action" label still holds
without re-checking. This is a real re-check, not a restatement.

Observation: this session's environment sets AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY to the
literal placeholder string "proxy-injected" (confirmed via `env | grep AWS_`), which is not
present in any earlier round's evidence. Rather than assume this was either live credentials or
inert, it was tested directly against real AWS KMS.

Test performed:
  VERIQO_AWS_KMS_INTEGRATION_TEST=1 \
  VERIQO_AWS_KMS_TEST_REGION=us-east-1 \
  VERIQO_AWS_KMS_TEST_KEY_ID=arn:aws:kms:us-east-1:000000000000:key/00000000-0000-0000-0000-000000000000 \
  VERIQO_AWS_KMS_TEST_ALGORITHM=ECDSA_SHA_256 \
  go test ./pkg/platform/security/keys/awskms/... -run TestAWSKMSIntegration -v

Result (real network call, real AWS response, this package's own hand-rolled SigV4 signer):
  awskms_integration_test.go:97: DescribeKey: awskms: unexpected response from KMS:
  TrentService.DescribeKey: HTTP 400:
  {"__type":"UnrecognizedClientException","message":"The security token included in the
  request is invalid."}
  --- FAIL: TestAWSKMSIntegration (0.52s)

Separately, `curl -sS -m 10 -o /dev/null -w "HTTP %{http_code}\n" https://kms.us-east-1.amazonaws.com/`
returned HTTP 404 (AWS's own response to an unsigned request against the KMS endpoint root) --
confirming the network path to real AWS KMS IS reachable through this session's egress proxy; the
blocker is credentials, not network policy.

Conclusion: the placeholder AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY values present in this
environment are not usable AWS credentials -- AWS itself rejects them
(UnrecognizedClientException). This is real, freshly-obtained negative evidence, not an assumption
carried over from an earlier round: hsm_kms remains genuinely blocked on the procurement of a real
AWS account / KMS tenancy (or equivalent HSM), which no engineering effort inside this sandbox can
substitute for. pkg/platform/security/keys/awskms's SigV4 signer and provider are otherwise
complete and unit-tested (see awskms_test.go) and will work correctly the moment real credentials
and a real, provisioned asymmetric SIGN_VERIFY key are supplied via the same env vars this test
already reads.
