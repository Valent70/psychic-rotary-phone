# testdata/provenance/contracts

No real data-provider contract exists in this session. When a real
contract is signed with a maritime/commodity data provider, its
reference document (or a hash/pointer to it, never the raw legal text if
sensitive) belongs here, and `provenance/provider/*.json`'s
`contract_reference` field should point to it.
